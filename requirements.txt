# Core Dependencies - LangChain v1 (2025)
# Note: Requires Python 3.10+ for LangChain v1
langchain>=1.0.0  # Core abstractions (agents, models, messages, tools)
langchain-openai>=1.0.0  # OpenAI integration
langchain-groq>=0.1.0  # Groq API for open-source LLMs (Llama, Mixtral)
langchain-community>=0.4.0  # Community integrations
langchain-classic>=1.0.0  # Legacy agent APIs (fallback for backward compatibility)
langgraph>=1.0.0  # Graph-based agent orchestration (recommended for agents)
langgraph-prebuilt>=0.1.0  # Prebuilt components for LangGraph
openai>=1.12.0
groq>=0.4.0  # Groq SDK
pydantic>=2.0.0

# Database
# sqlite3 is built into Python - no external package needed

# Vector Store for Semantic Search
chromadb>=0.4.0
sentence-transformers>=2.2.0
langchain-chroma>=0.1.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Date/Time handling
python-dateutil>=2.8.2

# Environment variables
python-dotenv>=1.0.0

# CLI and Output
rich>=13.7.0
tabulate>=0.9.0

# Web Frontend
streamlit>=1.30.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Vision Language Model (VLM) Integration
opencv-python>=4.8.0  # Video frame extraction
Pillow>=10.0.0  # Image processing
transformers>=4.36.0  # BLIP-2 and other VLMs (requires GPU for full functionality)
# torch>=2.1.0  # Uncomment for GPU support (install separately based on your CUDA version)
# Note: For CPU-only, install: pip install torch --index-url https://download.pytorch.org/whl/cpu
# For CUDA, visit: https://pytorch.org/get-started/locally/

# Type checking (development)
# mypy>=1.8.0
